---
import Navbar from '../../../Navbar'
import data from '../../../../data.json' assert { type: 'json' }

const { title, description } = data.header

type Props = {
  structureId: string
}

const { structureId } = Astro.props
---

<header id={structureId} class='p-4 flex flex-col gap-4'>
  <Navbar client:only />
  <section class='flex flex-col items-center justify-center gap-4'>
    <h1 class='font-basicaline text-5xl'>{title}</h1>
    <p class='prose prose-lg dark:prose-invert'>{description}</p>
  </section>
</header>
